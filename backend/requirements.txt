# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32   # required for FastAPI file uploads

# ── PDF extraction ─────────────────────────────────────────────────────────────
pdfplumber==0.11.10        # primary PDF text extractor (text-based PDFs)
pypdf==6.14.2              # secondary PDF extractor / fallback before OCR

# ── OCR fallback (scanned PDFs) ────────────────────────────────────────────────
pdf2image==1.17.0          # converts PDF pages → PIL Images for OCR
pytesseract==0.3.13        # Python wrapper for Tesseract OCR engine
Pillow==12.3.0             # image processing (required by pdf2image & pytesseract)

# ── DOCX extraction ────────────────────────────────────────────────────────────
python-docx==1.2.0

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv==1.2.2       # loads .env config (e.g. TESSDATA_PREFIX)
